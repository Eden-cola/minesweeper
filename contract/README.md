# Online Minesweeper Contract

## Overview
在线扫雷游戏的后端与前端之间的契约定义，包含 GraphQL Schema 和共享类型。

## GraphQL Schema
API 定义使用 GraphQL，支持实时订阅（Subscriptions）用于游戏状态推送。

## Common Types
REST API 共享类型定义（如有）。

## Schema First
所有 API 必须先在此定义，再进行后端和前端开发。